import Cocoa

// type hinting
// let is the way to create constants (const in JavaScript)
let numberOfStopLights: Int = 4

var population: Int
population = 5422
let townName: String = "Night City"
let townElevation: Int = 5000
let townDescription: String = "\(townName) has a population of \(population) and \(numberOfStopLights) stoplights. The has a \(townElevation) meters elevation on the sea level"
print(townDescription)

- To create the requiremens.txt
```bash
pip freeze > requirements.txt
```

- Commands to launch like npm install
```bash
python3 -m venv .venv
source .venve/bin/activate
pip install -r requirements.txt
```
